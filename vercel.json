{
    "version": 2,
    "buildCommand": "npx prisma generate && next build",
    "builds": [
      {
        "src": "index.ts",
        "use": "@vercel/node"
      }
      
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/"
      }
    ]
  }